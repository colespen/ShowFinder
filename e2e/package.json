{
  "name": "showfinder-e2e",
  "private": true,
  "description": "End-to-end tests for the ShowFinder client and API",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
